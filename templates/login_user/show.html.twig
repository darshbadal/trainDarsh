{% extends 'base.html.twig' %}

{% block title %}LoginUser{% endblock %}

{% block body %}
    <h1>LoginUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ login_user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ login_user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ login_user.roles ? login_user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ login_user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('login_user_index') }}">back to list</a>

    <a href="{{ path('login_user_edit', {'id': login_user.id}) }}">edit</a>

    {{ include('login_user/_delete_form.html.twig') }}
{% endblock %}
